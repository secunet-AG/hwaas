[package]
name = "error_utils"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "error_utils"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
tracing.workspace = true
contextapi-workspace-hack = { version = "0.1", path = "../contextapi-workspace-hack" }
