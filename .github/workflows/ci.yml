# This file is generated. Do not edit it manually.
#
# Regenerate it with:
#
#   nix run .#generate-ci
concurrency:
  cancel-in-progress: true
  group: ci-${{ github.workflow }}-${{ github.ref }}
jobs:
  build-aruba-switch-mock:
    name: 'Build: aruba-switch-mock'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build aruba-switch-mock
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.aruba-switch-mock'
  build-contextapi:
    name: 'Build: contextapi'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build contextapi
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.contextapi'
      - if: always()
        name: build contextapi-oas
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.contextapi-oas'
      - if: always()
        name: build contextapi-version
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.contextapi-version'
  build-hunt:
    name: 'Build: hunt'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build hunt
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.hunt'
  build-net-ctrl:
    name: 'Build: net-ctrl'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build net-ctrl
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.net-ctrl'
      - if: always()
        name: build net-ctrl-client
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.net-ctrl-client'
      - if: always()
        name: build net-ctrl-oas
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.net-ctrl-oas'
  build-remote:
    name: 'Build: remote'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build remote-auxiliary
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.remote-auxiliary'
      - if: always()
        name: build remote-hands-oas
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.remote-hands-oas'
      - if: always()
        name: build remote-power
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.remote-power'
      - if: always()
        name: build remote-serial
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.remote-serial'
      - if: always()
        name: build remote-usb
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.remote-usb'
  build-rpi-status-display:
    name: 'Build: rpi-status-display'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build rpi-status-display
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.rpi-status-display'
  build-sbom:
    name: 'Build: sbom'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build sbom-aruba-switch-mock
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-aruba-switch-mock'
      - if: always()
        name: build sbom-contextapi
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-contextapi'
      - if: always()
        name: build sbom-hunt
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-hunt'
      - if: always()
        name: build sbom-machine-ops
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-machine-ops'
      - if: always()
        name: build sbom-net-ctrl
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-net-ctrl'
      - if: always()
        name: build sbom-remote-auxiliary
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-remote-auxiliary'
      - if: always()
        name: build sbom-remote-power
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-remote-power'
      - if: always()
        name: build sbom-remote-serial
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-remote-serial'
      - if: always()
        name: build sbom-remote-usb
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-remote-usb'
      - if: always()
        name: build sbom-rpi-status-display
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-rpi-status-display'
      - if: always()
        name: build sbom-ws-gateway
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-ws-gateway'
      - if: always()
        name: build sbom-ws-proxy-client
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.sbom-ws-proxy-client'
  build-ungrouped:
    name: 'Build: ungrouped'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build documentation
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.documentation'
      - if: always()
        name: build http-sim
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.http-sim'
      - if: always()
        name: build hwaas-oas
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.hwaas-oas'
      - if: always()
        name: build machine-ops
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.machine-ops'
      - if: always()
        name: build ws-proxy-client
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.ws-proxy-client'
  build-user-tooling:
    name: 'Build: user-tooling'
    needs:
      - check-user-tooling
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build user-tooling-benchmarkDataCollector
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.user-tooling-benchmarkDataCollector'
      - if: always()
        name: build user-tooling-hwaasDataUpload
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.user-tooling-hwaasDataUpload'
      - if: always()
        name: build user-tooling-hwaasPythonDriver
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.user-tooling-hwaasPythonDriver'
      - if: always()
        name: build user-tooling-hwaasTimer
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.user-tooling-hwaasTimer'
  build-vue-client:
    name: 'Build: vue-client'
    needs:
      - check-vue-client
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build vue-client
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.vue-client'
  build-ws-gateway:
    name: 'Build: ws-gateway'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: build ws-gateway
        run: nix build --accept-flake-config -L --show-trace '.#packages.x86_64-linux.ws-gateway'
  check-cargo:
    name: 'Check: cargo'
    needs: []
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: check cargo-clippy
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.cargo-clippy'
      - if: always()
        name: check cargo-deny
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.cargo-deny'
      - if: always()
        name: check cargo-docs
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.cargo-docs'
      - if: always()
        name: check cargo-hakari
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.cargo-hakari'
      - if: always()
        name: check cargo-nextest
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.cargo-nextest'
  check-openapi-spec:
    name: 'Check: openapi-spec'
    needs: []
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: check openapi-spec-contextapi-golden-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.openapi-spec-contextapi-golden-test'
      - if: always()
        name: check openapi-spec-net-ctrl-golden-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.openapi-spec-net-ctrl-golden-test'
      - if: always()
        name: check openapi-spec-remote-hands-golden-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.openapi-spec-remote-hands-golden-test'
  check-pre-commit:
    name: 'Check: pre-commit'
    needs: []
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: check pre-commit
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.pre-commit'
  check-user-tooling:
    name: 'Check: user-tooling'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: check user-tooling-checkTestconfig
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.user-tooling-checkTestconfig'
  check-verify-ci:
    name: 'Check: verify-ci'
    needs: []
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: check verify-ci
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.verify-ci'
  check-vue-client:
    name: 'Check: vue-client'
    needs:
      - check-pre-commit
      - check-verify-ci
      - check-cargo
      - check-openapi-spec
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: check vue-client-licenses-check
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.vue-client-licenses-check'
  test-aruba-switch-mock:
    name: 'Test: aruba-switch-mock'
    needs:
      - build-aruba-switch-mock
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: test aruba-switch-mock-integration-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.aruba-switch-mock-integration-test'
      - if: always()
        name: test aruba-switch-mock-login-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.aruba-switch-mock-login-test'
      - if: always()
        name: test aruba-switch-mock-ports-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.aruba-switch-mock-ports-test'
      - if: always()
        name: test aruba-switch-mock-stats-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.aruba-switch-mock-stats-test'
  test-contextapi:
    name: 'Test: contextapi'
    needs:
      - build-contextapi
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: test contextapi-cors-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-cors-test'
      - if: always()
        name: test contextapi-images-drives-and-middleware
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-images-drives-and-middleware'
      - if: always()
        name: test contextapi-lifetime-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-lifetime-test'
      - if: always()
        name: test contextapi-net-api-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-net-api-test'
      - if: always()
        name: test contextapi-remote-hands-aux-device-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-remote-hands-aux-device-test'
      - if: always()
        name: test contextapi-remote-hands-routing-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-remote-hands-routing-test'
      - if: always()
        name: test contextapi-remote-serial-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-remote-serial-test'
      - if: always()
        name: test contextapi-startup-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-startup-test'
      - if: always()
        name: test contextapi-test-open-telemetry
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-test-open-telemetry'
      - if: always()
        name: test contextapi-ws-network-routing-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.contextapi-ws-network-routing-test'
  test-hunt:
    name: 'Test: hunt'
    needs:
      - build-hunt
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: test hunt-rust-log
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.hunt-rust-log'
  test-net-ctrl:
    name: 'Test: net-ctrl'
    needs:
      - build-net-ctrl
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: test net-ctrl-get-switches
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.net-ctrl-get-switches'
      - if: always()
        name: test net-ctrl-test-open-telemetry
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.net-ctrl-test-open-telemetry'
      - if: always()
        name: test net-ctrl-test-switch-client-cache
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.net-ctrl-test-switch-client-cache'
  test-remote:
    name: 'Test: remote'
    needs:
      - build-remote
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: test remote-hands-check-otel-traces
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-check-otel-traces'
      - if: always()
        name: test remote-hands-remote-auxiliary
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-remote-auxiliary'
      - if: always()
        name: test remote-hands-remote-power
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-remote-power'
      - if: always()
        name: test remote-hands-remote-serial-echo
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-remote-serial-echo'
      - if: always()
        name: test remote-hands-remote-serial-udev-custom-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-remote-serial-udev-custom-test'
      - if: always()
        name: test remote-hands-remote-serial-udev-default-test
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-remote-serial-udev-default-test'
      - if: always()
        name: test remote-hands-remote-usb
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.remote-hands-remote-usb'
  test-ws-gateway:
    name: 'Test: ws-gateway'
    needs:
      - build-ws-gateway
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Use cache
        uses: cachix/cachix-action@v17
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: secunet-ag-hwaas
      - if: always()
        name: test ws-gateway-test-clients-and-suts-scenario
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.ws-gateway-test-clients-and-suts-scenario'
      - if: always()
        name: test ws-gateway-test-connect
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.ws-gateway-test-connect'
      - if: always()
        name: test ws-gateway-test-many-clients-scenario
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.ws-gateway-test-many-clients-scenario'
      - if: always()
        name: test ws-gateway-test-many-vlans-ping
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.ws-gateway-test-many-vlans-ping'
      - if: always()
        name: test ws-gateway-test-open-telemetry
        run: nix build --accept-flake-config -L --show-trace '.#checks.x86_64-linux.ws-gateway-test-open-telemetry'
name: CI
'on':
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}
permissions:
  contents: read
